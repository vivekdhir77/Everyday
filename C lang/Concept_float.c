// we can use this program for correcting to decimals
#include<stdio.h>
int main(){
    float div=24.0/11.0;
    printf("%.10f\n",div);/* %.10f will correct the decimal
    value to 10 decimal places */
    /* the correction depends on the value %.12f */}