#include<iostream>
#include<cmath>
#define max(a,b) (a>b?a:b)
#define min(a,b) (a>b?b:a)
#define li long int
#define ll long long int

using namespace std;
ll array[1000000001];

int main()

{
    ll n;
    for (int i=0; i<10; i++){
        cin>>n;
        solve(n);
    }
    
}
void solve(ll n){
        if ((a+b+c)==n)
        {
        ans[i]=n;
        cout<<ans[i]<<endl;
        }
}
